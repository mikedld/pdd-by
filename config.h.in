#ifndef CONFIG_H
#define CONFIG_H

#cmakedefine PDDBY_SHARE_DIR "${PDDBY_SHARE_DIR}"

#if __GNUC__ >= 4
#define GNUC_VISIBLE __attribute__((visibility("default")))
#else
#define GNUC_VISIBLE
#endif

#ifndef sqlite3_prepare_v2
#define sqlite3_prepare_v2 sqlite3_prepare
#endif

#endif // CONFIG_H
