Под Mac OS X, разумеется, нужна установка зависимостей из MacPorts (http://macports.org).
Для нормальной работы необходимы два workaround'a:

1) Собрать из портов cairo и pango с +quartz
2) в ~/.gtkr-2.0 указать шрифт, с которым будут видны русские символы, например:
    gtk-font-name="geneva"
